shallow_clone: true

version: 0.3.{build}

image: Visual Studio 2015

platform:
  - x64

configuration:
  - Release

install:

before_build:
  - mkdir build
  - cd build
  - cmake .. -G "Visual Studio 14 2015 Win64" -DAUTOBUILD_ZMQ=ON -DBUILD_RELEASE_ONLY=ON -DBUILD_C_SHARED_LIB=ON -DBUILD_HELICS_EXAMPLES=ON
  - cd ..

build:
  project: build/HELICS.sln
  parallel: true
  verbosity: minimal

after_build:

artifacts:
