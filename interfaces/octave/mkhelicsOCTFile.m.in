mkoctfile('-I${LIBRARY_INCLUDE_LOCATION}','-I${OTHER_INCLUDE_LOCATION}', '-L${LIBRARY_BUILD_LOCATION} -l${LIBRARY_NAME} "-Wl,-rpath,./:../bin/:${LIBRARY_BUILD_LOCATION}" -o${BUILD_DIR}/helics.oct','${BUILD_FILE}')
