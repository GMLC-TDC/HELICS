#ifndef HELICS_CONFIG_H
#define HELICS_CONFIG_H
#pragma once

#include "compiler-config.h"

#cmakedefine01 HELICS_HAVE_MPI

#cmakedefine01 HELICS_HAVE_ZEROMQ

#cmakedefine DISABLE_TCP_CORE

#cmakedefine DISABLE_LOGGING
#cmakedefine DISABLE_TRACE_LOGGING
#cmakedefine DISABLE_DEBUG_LOGGING

#cmakedefine BOOST_STATIC

#define BOOST_VERSION_LEVEL @BOOST_VERSION_LEVEL@

#define HELICS_VERSION_MAJOR ${HELICS_VERSION_MAJOR}
#define HELICS_VERSION_MINOR ${HELICS_VERSION_MINOR}
#define HELICS_VERSION_PATCH ${HELICS_VERSION_PATCH}
#define HELICS_VERSION_BUILD "${HELICS_VERSION_BUILD}"
#define HELICS_VERSION_STRING "${HELICS_VERSION_STRING}"
#define HELICS_DATE "${HELICS_DATE}"

#endif
