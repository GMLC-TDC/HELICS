//this should be a valid json file (except comments are not recognized in standard JSON)
{
  //example json connection file  connections can be an array of two element string arrays  ["publication", "input"]
  "connections": [["pub1", "inp1"]]
}
