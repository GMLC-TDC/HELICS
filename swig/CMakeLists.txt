if (GENERATE_PYTHON OR GENERATE_MATLAB OR GENERATE_JAVA OR GENERATE_OCTAVE)
		FIND_PACKAGE(SWIG 3.0)
        INCLUDE(${SWIG_USE_FILE})
endif()

if (BUILD_PYTHON OR BUILD_MATLAB OR BUILD_JAVA OR BUILD_OCTAVE)
	INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
	INCLUDE_DIRECTORIES("${PROJECT_SOURCE_DIR}/src/helics/shared_api_library")
endif()
	

if (BUILD_PYTHON OR GENERATE_PYTHON)
    ADD_SUBDIRECTORY(python)
endif()

if (BUILD_JAVA OR GENERATE_JAVA)
    ADD_SUBDIRECTORY(java)
endif ()

if(BUILD_MATLAB OR GENERATE_MATLAB)
    FIND_PACKAGE(MATLAB REQUIRED)
    add_subdirectory(matlab)
endif()

if (BUILD_OCTAVE OR GENERATE_OCTAVE)
	add_subdirectory(octave)
endif()