filter=-readability/casting # this is picking up a number of false positives
filter=-runtime/int # this is picking up a few items in the tests that are meant to be there
filter=-build/namespaces # this is just annoying if not allowed in the tests
